<!DOCTYPE html>
<html lang="en">
    <head>

        <% include ../partials/head %>

        <title>Working</title>
    </head>
    <body>

        <% include ../partials/header %>

        <!-- main body -->
        <main>
            <div class="center-align">
                </br>
                <img class="middle" src="../resources/pictures/work-in-progress.png" alt="Work in progress">
                <p><%=i18n.__('Hey')%> ! <%=i18n.__('It_seems')%> !</p>
                <p><%=i18n.__('Sorry_but')%>.</p>
                <p><%=i18n.__('But_let')%> ! <%=i18n.__('And_see')%> !</p>
            </div>
        </main>

        <% include ../partials/footer %>

        <% include ../partials/foot %>

    </body>
</html>